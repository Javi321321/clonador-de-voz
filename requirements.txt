# Instala primero PyTorch según tu hardware (ver README), luego el resto.
# IMPORTANTE: se fija <2.9 a propósito — a partir de esa versión, torchaudio
# necesita la librería torchcodec, que en Windows requiere instalar FFmpeg
# como DLLs sueltas por separado. Evitamos todo ese lío quedándonos en la
# rama estable anterior, que sigue funcionando perfecto para este proyecto.
#
#   CPU (recomendado en laptops de bajos recursos):
#     pip install "torch<2.9,>=2.1.0" "torchaudio<2.9,>=2.1.0" --index-url https://download.pytorch.org/whl/cpu
#
#   GPU NVIDIA (recomendado en notebooks gamer con CUDA):
#     pip install "torch<2.9,>=2.1.0" "torchaudio<2.9,>=2.1.0" --index-url https://download.pytorch.org/whl/cu121
#
#   pip install -r requirements.txt

sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24
psutil>=5.9.0
faster-whisper>=1.0.0
transformers>=4.40.0,<5.0.0
sentencepiece>=0.2.0
coqui-tts>=0.24.0
piper-tts>=1.2.0
